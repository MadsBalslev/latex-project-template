\documentclass[12pt]{article}

\usepackage[danish]{babel} % For Danish writing.
\usepackage[utf8]{inputenc}
\usepackage[a4paper, margin=4cm]{geometry} % margins.

% Adds support for full page background picture
\usepackage[contents={},color=gray]{background}

\usepackage{xcolor}
\definecolor{aaublue}{RGB}{33,26,82}% dark blue
\definecolor{bg}{HTML}{f2f2f2}

%\usepackage[pdftex]{graphicx} % For pictures.

\usepackage{amssymb,amsmath,amsthm,amsfonts} % Math.

\usepackage{minted} % For code blocks
\usemintedstyle{emacs}
\setminted{
    frame=leftline,
    framesep=2mm,
    baselinestretch=1.2,
    fontsize=\footnotesize,
    bgcolor=bg,
    linenos
}

\usepackage{hyperref} % Format links for pdf.
\usepackage{lastpage} %Bruge til at få sidetal fra sidste side
\usepackage{calc} % Reset counter after title page.

% Bibliografi
\usepackage{csquotes} 
\usepackage[backend=biber]{biblatex}
\bibliography{bib.bib}

% Ændr titel på indholdfortegnelse
\addto\captionsdanish{\renewcommand{\contentsname}{Indholdsfortegnelse}}

